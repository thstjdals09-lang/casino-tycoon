# Casino Tycoon 작업 메모

새 세션을 시작하면 아래 "진행 상황 로그"부터 읽고 이어서 작업할 것. 세션을 마칠 때는 맨 위에 날짜와 함께 새 항목을 추가한다 (무엇을 했는지 / 다음에 할 일).

## 진행 상황 로그

### 2026-09-11 (44차) — 버그 수정: 뽑기 결과창이 뽑기 팝업 뒤에 가려지던 문제
- "뽑기" 큰 팝업이 뜬 상태에서 가챠를 뽑으면, 뽑기 결과 모달이 같은 z-index라 DOM 순서상 뽑기 팝업보다 먼저 그려져서 뒤에 가려짐(뽑기 팝업을 닫아야만 보임). 렌더 순서를 바꿔 뽑기 결과 모달이 항상 맨 위에 쌓이도록 수정.
- `npm run build` 통과 확인.

### 2026-09-11 (43차) — 전체강화 버튼 + 서빙 직원 동선 + 뽑기/딜러 탭을 큰 팝업으로 전환
- **전체 강화**: 테이블 탭에 "🔧 전체 테이블 강화", 뽑기 팝업에 "🔧 전체 딜러 강화" 버튼 신설 — 보유한 걸 하나하나 안 눌러도 선택된 배수(x1/x10/x100/MAX)만큼 한 번에 전부 강화됨.
- **서빙 직원 동선**: 기존엔 바 앞에서 제자리 왕복만 하던 것 → 이제 실제로 바에서 음료(🥤)를 들고 임의의 테이블까지 걸어가서 잠깐 서빙하고 다시 바로 돌아오는 걸 반복(테이블 위치는 매번 새로 조회해서 층이 바뀌어도 자연스럽게 따라감).
- **뽑기/딜러 탭 → 큰 팝업창으로 전환**: 하단 좁은 패널 안에 끼워져 있던 "딜러"(→ 이제 "뽑기"로 이름 변경) / "도감"(→ 이제 "딜러"로 이름 변경) 탭 내용을 화면 중앙의 큰 팝업(94vw, 최대 640px, 높이 82vh)으로 분리. 하단 네비게이션 4번째 자리는 그대로 "매장"이 차지하고, 테이블/매장만 기존처럼 하단 패널에 남음. 팝업이 떠 있어도 게임 화면은 계속 뒤에서 정상 동작(자동수익, 서빙 등 전부 계속 진행).
- `npm run build` 통과 확인. (세이브 변경 없음, UI/게임플레이 개선)

### 2026-09-11 (42차) — 후반 난이도 상향(초반 유지) + 미션 어뷰징 수정 + 주간/월간 목표 대폭 상향
- **난이도**: 0티어(초반)는 그대로 두고 1~5티어의 테이블/딜러 강화 비용 성장률과 매장 확장 비용을 전부 상향(성장률 +0.08~0.16, 확장비용 2.4~3.4배). 인테리어/바 업그레이드 성장률도 소폭 상향. 초반 진입 경험은 손대지 않고 중후반 그라인드만 늘림.
- **미션 어뷰징 수정**: x100/MAX로 한 번에 대량 강화하면 그게 통째로 미션 진행도에 다 반영되던 문제 → 이제 강화는 몇 단계를 올렸든 "버튼 누른 횟수" 1회로만 카운트되게 수정(자동 업그레이드로 발생하는 강화는 아예 미션에 반영 안 됨).
- **주간/월간 미션 목표 대폭 상향**: 주간(채팅 5→20, 가챠 15→60, 강화 25→150), 월간(채팅 15→60, 가챠 50→250, 강화 80→600) — 하루 만에 다 깰 수 없도록 늘림. 보상 다이아도 비례해서 상향.
- `npm run build` 통과 확인. (세이브 구조 변경 없음, 이번엔 강제 초기화 안 함)

### 2026-09-11 (41차) — 연속 뽑기 방식 변경(선택한 횟수로 반복) + 성급 비용 누진 + 팝업 바깥 클릭 닫기 + 재화 속도 하향 + 전체 강제 초기화
- **연속 뽑기 방식 변경**: 기존엔 체크박스 켜면 배경에서 조용히 1개씩 계속 뽑던 방식 → 이제 체크박스를 켠 상태에서 "10회"를 누르면 10회 단위로, "100회"를 누르면 100회 단위로 계속 반복(약 1.1초 간격, 다이아 소진 시 자동 정지). 반복 중인 버튼은 "⏸ 반복 중"으로 표시되고 다시 누르면 정지. 별도의 "⏹ 연속 뽑기 정지" 버튼도 추가.
- **성급 업그레이드 비용 누진**: 기존엔 등급별 고정 개수(N/R 2개, SR/SSR 1개)만 있었는데, 이제 별이 오를수록 필요 개수가 늘어남(1★→2★는 기본값, 2★→3★는 2배, 3★→4★는 3배...). 등급별 기준값은 그대로 차등 유지.
- **팝업 바깥 클릭 시 닫기**: 설정/뽑기결과/웰컴백/이벤트/미션/랭킹 모달 전부 어두운 배경(바깥쪽)을 누르면 자동으로 닫힘. 전직 선택처럼 반드시 골라야 하는 모달은 제외.
- **재화 속도 하향**: 바의 다이아 산출 공식을 기존 지수형(레벨이 조금만 올라도 폭증)에서 선형으로 전면 교체해서 훨씬 천천히 쌓이게 함. 가챠 고정가도 💎10 → 💎50으로 인상. "컨텐츠(딜러 도감)가 너무 금방 마른다"는 피드백 반영.
- **전체 계정 강제 초기화 장치**: `contentPatchVersion` 마커 도입 — 클라우드 세이브의 값이 현재 패치 버전보다 낮으면(기존 계정 전부 해당) 병합하지 않고 완전히 새 세이브로 시작시킴. 이번 패치로 기존 테스터 전원의 데이터가 자동으로 초기화됨(다음 접속 시).
- `npm run build` 통과 확인.

### 2026-09-11 (40차) — 미션/랭킹을 좌측 아이콘으로 분리 + 이벤트→미션 바로가기
- 매장 탭에 있던 "오늘/주간/월간 미션"과 "실시간 랭킹" 섹션을 완전히 제거하고, 화면 좌측에 🎁 이벤트 아래로 📋 미션 / 🏆 랭킹 아이콘을 추가(총 3개 아이콘, 각각 눌러서 모달로 확인).
- 🎁 이벤트 모달 안의 미션 진행 상황 줄을 클릭하면 이벤트 모달이 닫히고 바로 📋 미션 모달로 이동(수령까지 그 자리에서 가능).
- `npm run build` 통과 확인. (세이브 변경 없음, UI 위치만 재배치)

### 2026-09-11 (39차) — 등급별 뽑기 이펙트 차등화
- 뽑기 카드/플래시 연출을 등급이 높을수록 화려해지게 개편. N은 기본 페이드인 그대로, R은 살짝 튕기며 등장, SR은 더 크게 튕기고 보라색 광채가 은은히 지속 펄스 + 반짝이 아이콘, SSR은 배경 골드 그라디언트 + 빛줄기(rays) 회전 + 강한 골드 광채 펄스 + 브라이트니스 플래시가 섞인 화려한 등장.
- 텍스트 플래시(하단 알림)도 등급별로 강조: SR은 살짝 확대 펄스 2회, SSR은 더 크게+글로우 펄스 3회.
- `npm run build` 통과 확인. (세이브 변경 없음, 순수 연출)

### 2026-09-11 (38차) — 뽑기 연출 스킵 + 연속 뽑기(자동) 체크박스
- 딜러 탭 상단에 체크박스 2개 추가: **연출 스킵**(켜면 뽑기 결과 카드 모달 없이 바로 결과만 반영, 마지막 결과 플래시만 표시) / **연속 뽑기(자동)**(켜면 다이아가 있는 동안 1초 간격으로 조용히 자동 가챠를 계속 돌림, 카드 연출 없음).
- 가챠 핵심 로직을 `pullOnce()`(연출/최근결과 갱신 없음)로 분리하고, 수동 단일뽑기(`pullDealer`)/멀티뽑기(`pullDealerMultiple`)/자동뽑기(`autoPull`)가 각각 이를 감싸는 구조로 정리 — 자동뽑기가 화면에 매초 카드 모달을 띄우는 스팸을 방지.
- 세이브 버전 13 → 14 (skipGachaAnimation, autoPullEnabled 필드 추가).
- `npm run build` 통과 확인.

### 2026-09-11 (37차) — 다이아몬드 재화 + 고정가 가챠 + 일/주/월간 미션 + 멀티 뽑기 + 뽑기 연출
- **다이아몬드 신설**: 바가 이제 현금 대신 다이아몬드(💎, 가챠 전용 재화)를 초당 산출. 딜러 가챠 비용을 현금에서 다이아로 완전히 교체.
- **가챠 고정가**: 기존엔 보유 딜러 수에 따라 가격이 계속 오르던 방식 → 이제 1회당 💎10 고정가로 변경(더 이상 오르지 않음).
- **1/10/30/100 연차 뽑기**: 딜러 탭에 네 가지 뽑기 버튼 신설, 다이아가 부족하면 중간에 멈추고 결과만 반영.
- **뽑기 연출 모달**: 뽑을 때마다 결과를 카드 그리드로 순차 페이드인(스태거드 애니메이션) 표시. 카드마다 등급색 테두리 + 초상화 + NEW/중복 태그, 상단에 "신규 N명 · 중복 N개" 요약.
- **일/주/월간 미션**: 기존 일일 미션 3종(채팅/가챠/강화)에 주간·월간 버전 추가(목표치·보상 더 큼), 전부 다이아 보상으로 통일. 주간은 매주, 월간은 매달 자동 리셋.
- 세이브 버전 12 → 13 (diamonds, weeklyMissionProgress/Claimed, monthlyMissionProgress/Claimed, lastWeekKey/lastMonthKey 필드 추가). 병합 로직 덕분에 기존 진행상황은 유지됨.
- `npm run build` 통과 확인.

### 2026-09-11 (36차) — 딜러 시스템 대개편: 중복 재고 + 수동 성급 업그레이드, 로스터 44명
- **같은 딜러 중복 배치 불가**: 이제 딜러는 템플릿당 딱 1개체만 존재. 이미 보유한 딜러를 또 뽑으면 새 자리를 만드는 대신 "중복 재고"로 쌓임(더 이상 같은 이름 딜러를 여러 테이블에 나눠 배치할 수 없음).
- **수동 성급 업그레이드 버튼**: 별 등급이 보유 개수에 따라 자동으로 오르던 방식 → 딜러 탭에 "⭐ 성급 업" 버튼 신설, 중복 재고를 직접 소모해서 눌러야 오름. 등급별 필요 재고 개수 차등: N/R은 별 하나당 2개, SR/SSR은 1개만 있으면 업그레이드 가능(희귀할수록 적게 필요).
- 가챠 결과 메시지도 "신규 획득!" / "중복! 중복재고 +1" 로 구분 표시.
- **딜러 로스터 확장**: 32명 → **44명** (N 16종/R 14종/SR 8종/SSR 6종, 뽑기 확률 비율 유지).
- `DealerInstance`에 `stars` 필드(직접 저장), `GameSaveData`에 `dupeStock`(템플릿별 중복 재고) 필드 추가.
- 세이브 버전 11 → 12. 지난 턴에 고친 병합 방식 덕분에 기존 진행상황(현금/테이블/보유 딜러 등)은 최대한 보존되고, 새 필드만 기본값으로 채워짐.
- `npm run build` 통과 확인.

### 2026-09-11 (35차) — 세이브 유실 버그 수정 + 화면 꽉 채우기 + 딜러 로스터 32명으로 확장
- **버그(진짜 원인)**: "링크 나갔다 들어오면 저장이 하나도 안 된다" → `hydrateFromCloud()`가 클라우드 세이브의 `version`이 현재 `SAVE_VERSION`과 정확히 일치하지 않으면 통째로 버리고 새 세이브로 덮어쓰고 있었음. 이번 세션에서 세이브 포맷을 여러 번 바꾸면서(버전을 자주 올림) 배포할 때마다 기존 진행상황이 초기화되고 있었던 것. 이제 버전이 달라도 기본값 위에 클라우드 데이터를 병합(merge)하는 방식으로 바꿔서, 앞으로 세이브 구조를 바꿔도 기존 필드는 최대한 보존됨.
- **화면 꽉 채우기**: Phaser 내부 해상도를 800×480(가로로 넓은 비율) → 800×1000(세로로 긴 비율)로 변경. 모바일 세로 화면(좁고 긴 비율)에 훨씬 잘 맞아서 위아래 여백(레터박스)이 크게 줄어듦.
- **딜러 로스터 확장**: 16명 → 32명(최종 층 최대 테이블 수와 동일한 숫자). N 12종/R 10종/SR 6종/SSR 4종으로 뽑기 확률 비율에 맞춰 흔한 등급일수록 다양하게, 희귀 등급일수록 소수정예로 구성.
- 참고: "최대 테이블 수보다 딜러가 적으면 어쩌나" 하는 부분은, 같은 딜러를 중복으로 뽑아 여러 테이블에 배치하는 게 원래 허용되는 구조라 문제없음(중복 뽑기는 오히려 별 등급 시스템으로 보너스가 됨) — 다만 로스터 자체도 32명으로 늘려서 최종 층에서 이론상 전부 다른 딜러로만 채우는 것도 가능하게 함.
- `npm run build` 통과 확인. (세이브 병합 로직 덕분에 이번엔 기존 진행상황이 최대한 보존됨)

### 2026-09-11 (34차) — 탭(클릭) 수익 시스템 전면 제거 + 딜러 특기 개편 + UX 개선 여러 건
- **탭 수익 제거**: "탭당 골드 받는거 전부 없애" 요청 반영 — 테이블 클릭 시 보너스 캐시 지급 메커니즘(`tapTable`) 완전 삭제. 관련 상수(TAP_COOLDOWN_MS 등), `TableInstance.lastTapAt` 필드, 클릭 인터랙션 전부 제거.
- **딜러 특기 개편**: 탭 제거로 의미 없어진 '탭 보너스' 특기를 '매장 디자인 보너스'(손님 등급운 향상)로 교체(이막내/오센스/임디자이너 특기 변경). 전직 시스템의 탭 보너스도 같은 방식으로 디자인 보너스로 교체.
- **일일 미션 개편**: "테이블 탭하기" 미션 → "채팅 보내기"로 교체(새로 만든 채팅 기능과 자연스럽게 연결). 채팅 전송 시 자동으로 미션 진행도 반영.
- **바 서빙 직원**: 바가 생기면(레벨 1 이상) 서빙 직원이 바 앞을 왔다갔다 걸어다님(레벨 2 이상이면 2명).
- **채팅 위젯 위치 변경**: 화면 상단 중앙 → 하단 네비게이션 바로 위 우측(다른 모바일 게임들처럼). 펼친 상태에서 게임 화면의 다른 빈 곳을 탭하면 자동으로 접힘.
- **게임 화면 확대**: 하단 HUD 패널 높이를 44vh → 36vh로 줄이고 네비게이션 바 여백도 축소해서 실제 게임 캔버스가 보이는 영역을 넓힘.
- 세이브 버전 10 → 11 (lastTapAt 필드 제거, missionProgress/missionClaimed의 tap 키 → chat 키로 변경).
- `npm run build` 통과 확인.

### 2026-09-11 (33차) — 닉네임(=매장명) 설정 기능
- 채팅/랭킹에 노출되는 이름을 로그인 아이디 대신 사용자가 직접 정하는 "매장 이름"으로 변경. `GameSaveData.venueName` 필드 신설(계정 생성 시 기본값은 로그인 아이디, 이후 자유롭게 변경 가능).
- 설정(⚙️) 모달에 "매장 이름(닉네임)" 변경 폼 추가 — 2~12자 제한, 저장하면 로컬+클라우드에 바로 반영되고 채팅/랭킹에도 즉시 적용됨.
- `chat.ts`/`leaderboard.ts`가 Firebase Auth 아이디 대신 `gameState.venueName`을 표시 이름으로 사용하도록 수정.
- 세이브 버전 9 → 10 (venueName 필드 추가로 인한 자동 마이그레이션 — 기존 로그인 시 새 세이브로 시작하며 로그인 아이디가 기본 매장명으로 설정됨).
- `npm run build` 통과 확인.

### 2026-09-11 (32차) — 계정별로 진행상황 분리 (클라우드 세이브)
- 지적: "계정별로 진행상황 다른 거 맞지?" → 확인해보니 아니었음(계정은 로그인 게이트 역할만 하고, 실제 세이브는 브라우저 로컬저장소 공용이었음). 진짜 계정별 분리로 수정.
- `src/game/cloudSave.ts` 신설: Firestore `saves/{uid}` 문서에 세이브 데이터 저장/로드.
- `GameState.hydrateFromCloud()`: 로그인 직후, 게임(Phaser/HUD) 초기화 전에 반드시 호출 — 이 계정의 클라우드 세이브가 있으면 그걸로 교체, 없으면(신규 계정) 로컬에 남아있을 수 있는 다른 계정의 잔여 데이터를 물려받지 않도록 새 세이브로 시작해서 클라우드에 올림. 이제 로컬저장소는 그냥 오프라인 캐시 역할.
- `save()`/`resetGame()`도 로그인된 계정이 있으면 클라우드에 함께 반영하도록 수정.
- `npm run build` 통과 확인. (기존 세이브가 있던 사람도 로그인 시점에 자동으로 클라우드로 업로드/동기화됨)

### 2026-09-11 (31차) — Firebase 백엔드 연결: 실서버 계정 + 실시간 채팅 + 랭킹
- 사용자가 Firebase 프로젝트(jinojino-6aba2) 생성 및 설정값 제공 → `src/firebase.ts`에 초기화 코드 추가, `firebase` SDK 설치.
- **계정**: 로컬 localStorage 게이트 → Firebase Authentication(이메일/비번)으로 교체. 아이디는 내부적으로 `아이디@casino-tycoon.local` 형식 이메일로 변환해서 사용(화면엔 항상 원래 아이디만 노출). 여러 기기/브라우저에서 같은 계정으로 로그인 가능해짐.
- **실시간 채팅** (`src/game/chat.ts`, `src/ui/ChatWidget.ts`): 게임 화면 상단에 접이식 위젯. 평소엔 최신 메시지 한 줄만 보이는 알약 모양, 탭하면 최근 50개 로그 + 입력창으로 펼쳐짐(▲ 접기로 다시 축소). Firestore `chat_messages` 컬렉션 실시간 구독.
- **실시간 랭킹** (`src/game/leaderboard.ts`): 15초마다 내 현재 상태(현금/누적수익/초당수익/층)를 Firestore `leaderboard` 컬렉션(uid別 문서)에 업로드, 초당수익 기준 상위 20명을 매장 탭에 실시간 표시(내 순위는 강조 표시).
- 설정 모달의 로그아웃도 Firebase signOut으로 교체.
- 참고: Firestore를 "테스트 모드"로 만들어서 현재는 30일간 인증 여부와 무관하게 열려있는 기본 규칙 상태 — 나중에 실제로 계속 쓸 거면 보안 규칙을 조여야 함(현재는 테스트 목적이라 그대로 둠).
- `npm run build` 통과 확인. (게임 세이브 자체는 여전히 로컬 localStorage — 계정마다 클라우드 동기화는 아직 안 됨, 채팅/랭킹만 서버 연동)

### 2026-09-11 (30차) — 테스트용 간단 계정 시스템(로그인/회원가입 게이트)
- `src/game/account.ts`: localStorage 기반의 아주 단순한 아이디/비번 계정 시스템(평문 저장, 이 브라우저에서만 유효 — 서버 없는 정적 사이트라 실제 인증이 아니라 테스트용 게이트임을 명시).
- `src/ui/AuthGate.ts`: 로그인/계정 만들기 토글이 있는 풀스크린 게이트 UI. 로그인된 계정이 없으면 게임 시작 전에 이 화면부터 뜸(링크로 들어오면 계정부터 만들어야 함).
- 설정(⚙️) 모달에 "계정" 섹션 추가: 현재 로그인된 아이디 표시 + 로그아웃 버튼(로그아웃하면 새로고침되면서 다시 로그인 게이트로 돌아감).
- 세이브 데이터 자체는 계정별로 분리하지 않고 브라우저 공용으로 유지(요청이 "최대한 간단하게 테스트용"이라 계정은 순수 게이트 역할만).
- `npm run build` 통과 확인.

### 2026-09-11 (29차) — 딜러 도감에 초상화 추가
- 도감 목록이 텍스트만 있어서 밋밋하다는 피드백 → 각 딜러 항목 좌측에 실제 도트 캐릭터 초상화(인라인 SVG)를 추가.
- `pixelart.ts`에 `gridToSvg()` 함수 신설(픽셀 격자를 DOM에 바로 넣을 수 있는 SVG 문자열로 변환), `dealerRoster.ts`에 `accessoryForGrade()`(SR=모자, SSR=왕관, 나머지는 맨몸 — 게임 화면의 실제 딜러 스프라이트와 동일 규칙) 추가. 보유한 딜러는 등급 색 그대로, 미보유는 흑백+어둡게 처리.
- `npm run build` 통과 확인. (세이브 변경 없음)

### 2026-09-11 (28차) — 딜러 중복 뽑기 → 별 등급(성급) 시스템
- 요청: "같은 딜러 중복 뽑으면 별 올라가는 성급 시스템, 티어별 차등" 반영.
- 딜러를 중복 보유(같은 템플릿 여러 명)하면 별 등급이 자동으로 오름 — 별 하나당 그 딜러의 특기 수치가 곱연산으로 증가. 등급별 상한/보너스 차등화: N·R은 최대 5성(별당 +10%/+15%), SR은 4성(+20%), SSR은 3성(+12%) — 희귀할수록 만성까지 필요한 개수는 적지만 별 하나의 무게가 큼.
- **만성 각성**: 특정 딜러가 등급별 상한 별에 도달하면 전체 수익에 영구 보너스(N +2% ~ SSR +12%)가 추가로 붙는 "각성 스킬" 발동.
- 딜러 탭/도감 탭에 별(★☆) 표시 추가, 도감엔 별 하나당 보너스 수치 + 만성 각성 효과까지 상세 표시.
- 참고: "새 테이블 사고 자동배치하면 배치가 안 된다"는 리포트는 코드 재검토 결과 버그가 아니라 의도된 동작으로 확인됨(보유 딜러 수 < 테이블 수일 때, 최적 조합을 위해 레벨 낮은 새 테이블을 일부러 마지막 순위로 미룸).
- `npm run build` 통과 확인. (세이브 포맷 변경 없음, 별 등급은 보유 개수에서 실시간 계산)

### 2026-09-11 (27차) — 사이드 버튼 반응형 + 아이콘 전용 → 아이콘+텍스트로 변경
- 지적: 사이드 토글이 아이콘만 있어서 뭔지 알기 힘들고, 화면 크기별로 안 맞았음.
- `#side-actions`/`#side-actions-left`와 `.side-btn`을 `clamp()` 기반 반응형 크기로 전면 교체(원형 → 알약형, 화면 폭에 따라 아이콘/글자 크기 자동 조절, 최대 폭 22vw로 제한).
- 아이콘 아래에 텍스트 라벨 추가: 🤖 "자동 ON/OFF", 🔥 "부스트"/"부스트 중"/"대기중"(+ 남은 초), 🎁 "이벤트". 상태에 따라 라벨 문구도 바뀜.
- `npm run build` 통과 확인.

### 2026-09-11 (26차) — 바 세부 업그레이드(메뉴/외형 3단계)
- 바 레벨이 오르면 실제로 판매 메뉴가 늘어남: Lv.1 생맥주/소주 → Lv.3 하이볼 → Lv.5 와인 → Lv.7 시그니처 칵테일 → Lv.10 프리미엄 위스키 → Lv.14 올드 빈티지 브랜디. 매장 탭에 현재 취급 메뉴 목록 표시.
- 바 외형도 레벨에 따라 3단계로 변경: Lv.1~4 기본 카운터 → Lv.5~9 칵테일바(더 넓고 병 종류 다양) → Lv.10+ 고급 라운지(금테 + 반짝이 이펙트). 게임 화면 상단 바 스프라이트가 실제로 바뀜.
- `npm run build` 통과 확인. (세이브 포맷 변경 없음)

### 2026-09-11 (25차) — 이벤트 모달 깨짐 버그 수정
- **버그**: 좌측 🎁 이벤트 버튼을 누르면 모달이 화면 중앙이 아니라 화면 왼쪽 끝에 글자 하나씩 세로로 쪼개진 채 깨져서 나왔음. 원인은 `#side-actions-left`에 걸린 `transform: translateY(-50%)` — CSS 스펙상 조상 요소에 transform이 있으면 그 안의 `position: fixed` 자손은 화면 전체가 아니라 그 조상 박스 기준으로 배치됨. 왼쪽 버튼 박스가 좁다 보니 모달이 그 안에 욱여넣어져 깨진 것.
- 수정: 이벤트 모달을 `document.body` 바로 아래 별도 루트로 분리해서 렌더링 — transform 조상 문제에서 완전히 벗어나 화면 중앙에 정상적으로 뜸.
- `npm run build` 통과 확인.

### 2026-09-11 (24차) — 사이드 액션 패널 + 딜러 자동배치 + 일괄 업그레이드(x1/x10/x100/MAX)
- **게임 화면 좌우 고정 패널** 신설 (`src/ui/SidePanels.ts`): 우측엔 게임플레이 토글(🤖 자동 업그레이드, 🔥 황금시간 부스트), 좌측엔 🎁 이벤트·출석 아이콘(출석 연속일수 + 오늘의 미션 진행 요약을 모달로 보여줌, 미션 완료 시 빨간 점 표시). 기존 HUD 탭 안에 있던 부스트 버튼/자동 업그레이드 토글은 제거하고 이쪽으로 이전.
- **딜러 자동배치** 버튼 (테이블 탭): 효율 높은 딜러부터 레벨 높은 테이블부터 순서대로 자동 매칭.
- **일괄 업그레이드**: 상단 x1/x10/x100/MAX 선택 칩 신설. 테이블 강화/딜러 교육/인테리어·바 업그레이드 버튼이 전부 선택된 배수만큼(또는 살 수 있는 만큼) 한 번에 처리됨.
- `npm run build` 통과 확인. (세이브 포맷 변경 없음)

### 2026-09-11 (23차) — 자동 업그레이드 + 일일 미션
- **자동 업그레이드**: 설정(⚙️) 메뉴에 토글 추가. 켜면 여유 자금이 생길 때마다 1초 간격으로 테이블 구매/강화, 딜러 강화, 인테리어/바 업그레이드를 자동 처리해서 하나하나 누를 필요 없어짐. 가챠 뽑기와 매장 확장은 의도적으로 자동화에서 제외(재미/의사결정 요소이므로 직접 눌러야 함).
- **일일 미션** 3종 신설 (매장 탭): 테이블 탭 10회 / 딜러 가챠 3회 / 강화 5회. 진행 바로 진행도 표시, 달성하면 현재 수익 기반 보상 수령 가능. 출석 보상과 같은 "새로운 날" 타이밍에 자동 리셋됨.
- 세이브 버전 v8 → v9 (autoUpgradeEnabled/missionProgress/missionClaimed 필드 추가, 구버전 세이브는 초기화됨).
- `npm run build` 통과 확인.

### 2026-09-11 (22차) — 딜러 로스터 개수를 뽑기 확률에 맞게 재조정
- 지적: "뽑기 확률 다른데 등급별 기물 수가 4개씩 똑같은 게 이상하지 않냐" → 맞는 지적. 흔한 등급(N 60%)은 종류를 늘려서 반복 체감을 줄이고, 희귀 등급(SSR 2%)은 종류를 줄여 소수정예로 만들어 "완전 수집" 목표가 현실적이게 재조정.
- N 6종 / R 5종 / SR 3종 / SSR 2종 (총 16명은 동일). SR은 gacha 특기, SSR은 tap/gacha 특기를 뺀 대신 income/bar 특기만 남겨 최상위 등급은 "돈/매출" 특화로 상징성을 줌.
- 세이브 버전 v7 → v8 (템플릿 id가 바뀌어서 구버전 세이브의 딜러 참조가 깨지는 걸 막기 위해 초기화).
- `npm run build` 통과 확인.

### 2026-09-11 (21차) — 이름 붙은 딜러 도감(팻도감 스타일) + 인테리어 시각화 + 배정 UX 개선
- **이름 붙은 딜러 로스터** 신설 (`src/game/dealerRoster.ts`): 등급별 4명씩 총 16명(N: 김대리/박신입/이막내/최수습, R: 정프로/한베테랑/오스피드/윤스카우터, SR: 서마스터/강칵테일/임퀵핸드/조인맥, SSR: 황전설/백소믈리에/노번개손/남인맥왕). 각자 고유 특기 하나씩 보유 — income/bar/tap 특기는 "배치효과"(테이블 배정 시만 적용), gacha 특기는 "보유효과"(보유만 해도 적용, 고급등급 확률 상승). 가챠 뽑을 때 등급 안에서 랜덤으로 어떤 이름의 딜러인지도 함께 결정됨(`DealerInstance.templateId`).
- **딜러 도감 전면 개편**: 기존엔 등급 4종만 보여주던 것을 16명 전부 개별 항목으로 표시하는 팻도감 스타일로 변경. 보유(해당 이름의 딜러를 한 번이라도 뽑음) 시 컬러+플레이버 텍스트, 미보유 시 무채색+🔒+"???"로 실루엣 처리. 보유효과/배치효과 구분 텍스트 유지.
- **딜러 배정 드롭다운**을 등급별 optgroup(SSR→SR→R→N 순)으로 그룹핑해서 스캔하기 쉽게 개선, 표시도 "딜러 #n" 대신 실제 이름으로.
- **인테리어 시각화**: 디자인 레벨이 오르면 실제로 매장에 장식이 늘어남 — Lv.3부터 화분, Lv.6부터 액자(레벨 오를수록 최대 4개까지 증가), Lv.10부터 샹들리에가 바 위에 걸림. 상단 좁은 데코 밴드에 칩/화분/액자를 한 줄로 섞어 배치.
- 세이브 버전 v6 → v7 (`DealerInstance.templateId` 필드 추가, 구버전 세이브는 초기화됨).
- `npm run build` 통과 확인.

### 2026-09-11 (20차) — 층 전환이 실제로 체감되게 보강
- 피드백: "바닥색만 바뀌는 게 의미가 있냐" → 맞는 지적. 아래 3가지 추가:
  1. **고정 층 표지판**: 화면 좌상단에 스크롤해도 항상 고정된(scrollFactor 0) "🏢 N층 / 티어명" 표지판 신설.
  2. **층 이동 토스트**: 실제로 매장을 확장하는 순간(최초 로드 시엔 안 뜨고, 진짜 확장했을 때만) 화면 상단에 "🎉 N층 입장 · 티어명" 큰 배너가 팝업 애니메이션과 함께 2초 정도 떴다 사라짐.
  3. **층별 화려함 증가**: 칩 장식 개수가 층(티어)이 오를수록 늘어나서(2개→최대 7개) 고층으로 갈수록 매장이 점점 화려해지는 게 보이게 함.
  4. 카메라 배경색도 층 색상에 맞춰 갱신.
- `npm run build` 통과 확인.

### 2026-09-11 (19차) — 타 방치형 타이쿤 게임 리서치 후 리텐션 기능 3종 추가
- 앱스토어의 인기 방치형 타이쿤 게임들(Idle Cash, Idle Restaurant Tycoon, Merge Miners, Mining Empire 등) 및 방치형 게임 리텐션 아티클을 리서치. 공통적으로 있는데 우리 게임엔 없던 요소: 오프라인 수익 웰컴백 팝업, 출석(데일리) 보상, 일시적 부스트 버튼. 3개를 추가함.
- **오프라인 수익 팝업**: 기존엔 콘솔에만 로그로 남기던 걸 "👋 어서오세요!" 모달로 접속할 때마다 보여줌 (오프라인 벌어들인 금액 표시).
- **출석 보상**: 날짜(로컬 기준)가 바뀌어 새로 접속하면 연속 출석일수 갱신하고, 현재 초당 수익 기반으로 스케일링되는 보상 지급(연속일수 최대 7일까지 배율 상승). 오프라인 수익 팝업과 같은 창에서 함께 안내.
- **황금시간 부스트**: 테이블 탭 맨 위에 버튼 신설. 60초간 전체 수익 2배, 재사용 대기 5분(부스트 시간 포함). 세이브하지 않는 일시적 상태라 새로고침하면 초기화됨(의도한 동작).
- 세이브 버전 v5 → v6 (lastLoginDate/loginStreak 필드 추가). `npm run build` 통과 확인.

### 2026-09-11 (18차) — 층별 시각 구분 + 스크롤, HUD 정리, 도감 탭 분리
- **층 구분**: 이전엔 매장 등급(층)이 달라져도 바닥 색이 고정이라 "다른 층"이라는 느낌이 없었음. 이제 층마다 `tier.floorColor` 기반으로 바닥 타일 색이 달라짐(1층 초록 → 2층 남색 → 3층 보라 → 4층 금색 → 5층 로즈 → 6층 틸).
- **스크롤 지원**: 테이블이 많아지는 고층(최대 32개)에서 화면 밖으로 넘치던 문제 → Phaser 카메라 bounds를 층별 필요 높이만큼 설정하고 세로 드래그로 스크롤 가능하게 함. 테이블 탭/구매 클릭은 드래그와 구분하기 위해 pointerdown 대신 pointerup + 이동거리(<8px) 체크로 변경. 층이 바뀌면 스크롤 맨 위로 리셋.
- 상단 HUD 잔고바에 "🏢 N/6층" 표시 추가.
- **HUD 정리**: 정보가 너무 많이 몰려있던 딜러 탭에서 "딜러 도감"(등급별 보유/배치 효과, 업적)을 별도 **도감 탭**으로 완전히 분리(하단 네비게이션 4탭: 테이블/딜러/도감/매장). 딜러 탭은 가챠+보유 딜러 목록만 남겨 훨씬 단순해짐.
- 도감 탭 효과 설명을 "👜 보유효과 / 🪑 배치효과 / 📈 레벨업 배율" 3줄로 명확히 구분해서 디테일하게 표시(레벨1 배치 시 예상 배율까지 계산해서 보여줌).
- `npm run build` 통과 확인. (세이브 포맷 변경 없음)

### 2026-09-11 (17차) — 테이블당 손님 8명 착석 (홀덤 8인 테이블 컨셉)
- `TableInstance.customerGrade`(단일) → `customerGrades`(최대 8명 배열)로 변경. 딜러 배정 시 매장 디자인 레벨 보너스를 적용해 8명을 한 번에 추첨.
- 테이블 수익 계산: 8명의 평균 씀씀이 배율 적용(한 명 등급에 좌우되지 않고 테이블 전체 분위기 반영, 밸런스는 기존과 비슷한 기대값 유지). 바 매출은 착석 손님 전체의 drinkMultiplier 합으로 계산해 손님이 많을수록 바 매출도 커지게 함.
- 도트: 테이블 둘레(타원)에 딜러 1명 + 손님 최대 8명을 9등분 각도로 배치해 실제 홀덤 8인 테이블처럼 보이게 구성. 인원이 늘어난 만큼 각 스프라이트는 축소(딜러 0.6배, 손님 0.34배), 레벨/수익 텍스트는 테이블 위쪽으로 이동해 겹침 방지.
- HUD 테이블 탭: 손님 배지를 "👥 8/8 (S×1 A×2 B×3 C×2)" 형태로 등급 구성 표시.
- 세이브 버전 v4 → v5. `npm run build` 통과 확인.

### 2026-09-11 (16차) — 장기 진행용 밸런스/콘텐츠 확장
- 업그레이드 비용 성장률을 수익 성장률보다 확실히 가파르게 상향(테이블/딜러 강화 비용 성장률 전 티어 +0.08~0.13). 여러 배율(등급/손님/업적/전직/디자인)이 곱연산으로 쌓이면서 예전 성장률로는 후반에 순식간에 다 찍어버리는 문제 완화.
- **매장 티어 2개 추가**: 4티어(아시아 투어 카지노, 테이블 24개), 5티어(월드클래스 홀덤 리조트, 테이블 32개, 최종 티어)로 로드맵 확장. 3티어(국내 최고 카지노)도 이제 최종이 아니고 확장 비용 4천만원 설정.
- 인테리어/바 업그레이드 비용도 레벨이 오를수록 성장률 자체가 조금씩 더 가팔라지도록 변경(무한 로그형 곡선 대신 체감 난이도 상승).
- 디자인 보너스 공식을 선형(level*0.09)에서 로그형(log2(level+1)*0.35)으로 바꿔서 무한정 찍어도 손님 등급이 무한히 좋아지진 않게(완만해짐) 조정.
- **테이블 레벨 마일스톤**: 10레벨마다 영구 +15% 수익 보너스 추가, 마일스톤 도달 시 레벨 옆에 ⭐ 표시(테이블 탭·게임 화면 둘 다).
- `npm run build` 통과 확인.

### 2026-09-11 (15차) — 테이블/딜러 목록 줄바꿈 조잡한 문제 수정
- 수익 숫자가 커지면서 `.row` 안에서 제목/드롭다운/버튼을 한 줄에 다 넣으려다 좁은 화면에서 글자 하나하나가 세로로 쪼개지던 문제. `.row`를 세로 스택(제목·부제 먼저, 그 아래 드롭다운+버튼)으로 재구성하고 select는 `flex:1`, 버튼은 `white-space:nowrap`으로 고정해 안 쪼개지게 함.
- `npm run build` 통과 확인.

### 2026-09-11 (14차) — 초기화 버튼 실제 버그 수정 + 설정 메뉴 신설
- **버그**: 초기화 버튼을 눌러도 아무것도 안 바뀌던 진짜 원인 — `resetSave()`로 localStorage를 지운 직후 `location.reload()`가 `beforeunload` 이벤트를 발생시켜, 거기 걸려있던 자동저장(`gameState.save()`)이 리셋 직전의 메모리 상태를 다시 그대로 써버렸음. `GameState.resetGame()`을 신설해 `autoSaveDisabled` 플래그를 켠 뒤 스토리지를 지우도록 수정 — 이제 진짜로 초기화됨.
- 상단바 우측에 ⚙️ 설정 버튼 신설. 누르면 설정 모달이 뜨고, "처음부터 다시 시작(전체 초기화)"를 매장 탭에서 이쪽으로 옮김.
- `npm run build` 통과 확인.

### 2026-09-11 (13차) — 초기화 버튼 추가
- 참고: "딜러 다 날아갔다"는 문제는 12차에서 고친 예전 프레스티지 리셋 동작 때문이었을 가능성이 높음(그 전엔 매장 확장 시 딜러가 전부 사라졌음). 지금은 고쳐져서 확장해도 딜러가 유지됨.
- 세이브가 브라우저 localStorage라 서버/코드 쪽에서 직접 지워줄 수 없어서, 매장 탭 맨 아래에 "처음부터 다시 시작(전체 초기화)" 버튼을 추가함. 확인창 뜨고 동의하면 세이브 삭제 후 새로고침.
- `npm run build` 통과 확인.

### 2026-09-11 (12차) — 프레스티지(초기화) 대신 누적 성장 구조로 변경
- 사용자 요청: "전생/리셋 시스템 대신 누적해서 수익을 버는 구조가 좋아 보인다" → 확인 결과 "초기화 없이 누적 성장(테이블/딜러/디자인/바 유지, 등급만 오름)" 방식 선택.
- **매장 확장 조건 변경**: 이전엔 현금이 advanceCost 이상이면 아무 때나 확장 가능했음 → 이제 "현재 층 테이블을 꽉 채워야"(tables.length >= maxTables) + 현금 충분해야 확장 가능. 즉 "테이블 수가 늘어나다가 맵이 꽉 차면 다음 층으로" 흐름.
- **확장해도 리셋 없음**: 현금(비용만 차감), 테이블, 딜러, 인테리어 레벨, 바 레벨 전부 그대로 유지한 채 venueTierIndex만 증가. prestigeMultiplier 보너스는 그대로 적용. 새 층은 maxTables가 더 커서 테이블을 더 살 수 있는 자리가 열리고, tableBaseIncome 등 티어 수치가 올라가 기존 테이블 수익도 함께 오름.
- HUD 매장 탭: 테이블이 안 찼으면 "테이블을 꽉 채우면 확장 가능" 안내, 다 찼으면 기존처럼 현금 진행바 + 확장 버튼(문구에서 "레벨 초기화" 문구 제거).
- `npm run build` 통과 확인. (세이브 포맷 변경 없음)

### 2026-09-11 (11차) — 강화 버튼 누르면 스크롤 위치 튀는 문제 수정
- 강화/교육 등 버튼을 눌러 상태가 바뀌면 `render()`가 `.tab-content`의 innerHTML을 통째로 교체하면서 스크롤이 맨 위로 리셋되던 문제 수정. 이제 렌더 직전 스크롤 위치를 저장했다가 렌더 후 그대로 복원. 단, 하단 탭(테이블/딜러/매장)을 새로 눌러 전환한 경우엔 새 탭이니 스크롤을 맨 위로 리셋.
- `npm run build` 통과 확인.

### 2026-09-11 (10차) — 진짜 원인: 딜러/테이블 ID 뒤바뀜 버그
- **진짜 버그**: `onChange` 핸들러에서 select의 `data-id`(테이블 id)를 딜러 id로, `value`(딜러 id)를 테이블 id로 거꾸로 읽고 있었음. 그래서 드롭다운에서 아무 딜러나 선택해도 내부적으로 존재하지 않는 테이블 id를 참조하게 돼 조용히 아무 일도 안 일어났음 (9차에서 고친 "목록에 안 보이는" 문제와는 별개의 훨씬 근본적인 버그였음 — 스크린샷으로 재현 확인 후 발견).
- `tableId = data-id`, `dealerId = value`로 순서 수정. "딜러 없음" 선택 케이스도 명시적으로 처리.
- `npm run build` 통과 확인.

### 2026-09-11 (9차) — 딜러 배정 드롭다운 버그 수정
- **버그**: 이미 다른 테이블에 배정된 딜러는 드롭다운 목록에서 아예 안 보이게 필터링돼 있었음. 그래서 보유한 딜러가 전부 다른 테이블에 배정된 상태에서 새 테이블에 배정하려 하면 "딜러 없음"만 보여서 아무것도 고를 수 없었음(= "딜러 교체가 안 된다"/"손님이 안 앉는다"의 원인).
- 수정: 모든 테이블의 드롭다운에 보유한 딜러 전체를 표시하고, 각 옵션에 현재 배정 상태(대기 중 / 이 테이블 / 테이블 #N에서 이동)를 라벨로 표시. 다른 테이블에 배정된 딜러를 선택하면 그 테이블에서 자동으로 빠지고 여기로 이동(기존 `assignDealer` 로직이 이미 스왑을 지원했음, UI에서 막고 있었던 것).
- `npm run build` 통과 확인.

### 2026-09-11 (8차) — 초반 밸런스 대폭 개선 (너무 느리다는 피드백 반영)
- 1티어(구석 테이블) 수치 전면 하향/상향 조정: 테이블 기본수익 1→3, 첫 테이블 구매비 50→25, 딜러 가챠 비용 80→35, 매장 확장 비용 5000→1200, 무딜러 효율 0.25→0.35. 체감상 시작하고 1분 이내에 딜러 뽑고, 몇 분 안에 첫 매장 확장(프레스티지)까지 가능하도록 튜닝.
- 2티어/3티어도 비례해서 완화(확장비 80000→25000, 1.5M→900K 등).
- 탭 쿨다운 3초→2초로 단축해 능동 플레이 피드백 속도 개선.
- `npm run build` 통과 확인. (세이브 포맷 변경 없음, 기존 세이브 유지됨)

### 2026-09-11 (7차) — 손님 등급/인테리어/미니바/도감 필터
- **손님 등급 시스템** 신설 (`src/game/customers.ts`): C(일반)/B(단골)/A(큰손)/S(VIP) 4등급, 등급별로 테이블 수익 배율(spendMultiplier)과 바 매출 배율(drinkMultiplier)이 다름. 딜러가 테이블에 배정될 때 현재 매장 디자인 레벨을 반영해 손님 등급을 추첨.
- **매장 인테리어(디자인) 업그레이드** (`src/game/decor.ts`): 매장 탭에서 구매 가능, 레벨이 오를수록 고급 손님(B/A/S) 등장 확률 상승. 매장 확장 시 초기화.
- **미니바 시스템**: 매장 탭에서 업그레이드, 레벨이 오를수록 더 비싼 음료 판매(초당 매출 자동 발생, 착석한 손님 등급에 따라 매출 배율 상승). 매장 확장 시 초기화.
- **딜러 도감 개편**: 딜러 탭에 등급별 보유효과/배치효과/레벨업 배율을 모두 텍스트로 표시. 보유(누적 고용 1명 이상)한 등급은 컬러, 미보유 등급은 무채색+자물쇠 아이콘. 보유/미보유, 등급(N/R/SR/SSR) 필터 칩 추가.
- 테이블 탭에 착석한 손님 등급 배지 표시.
- 도트: 손님도 등급별 텍스처(색상 구분, S등급은 모자+반짝이)로 분리, 미니바 카운터(병+선반) 스프라이트 추가해 화면 상단에 고정 배치.
- 세이브 버전 v3 → v4 (customerGrade/designLevel/barLevel 필드 추가, 구버전 세이브는 초기화됨).
- `npm run build` 통과 확인.

### 2026-09-11 (6차) — 도감/업적 + 손님 착석 조건 수정 + 등급별 스프라이트 차등화
- 손님 착석 조건 수정: 딜러가 배정된 테이블에만 손님이 앉도록 원복(이전엔 딜러 유무 무관하게 전부 착석했었음). 말풍선도 딜러 있는 테이블에서만 뜨도록 함께 수정.
- **손님 착석 수익 보너스**: 딜러 배정(=손님 착석) 시 테이블 수익에 +15% 배율 추가 (`CUSTOMER_SEATED_BONUS`).
- **딜러 도감/업적 시스템** 신설 (`src/game/achievements.ts`): 등급별/누적 고용 수 기준 업적 7종(N 5명, R 5명, SR 3명, SSR 1명, SSR 3명, 누적 20명, 누적 50명), 달성 시 영구 전체 수익 배율 적용. 누적 고용 카운트(`dealerPulls`)는 프레스티지해도 초기화 안 됨. HUD 딜러 탭 하단에 도감 목록/진행 현황 표시, 새로 달성하면 딜러 탭에 플래시 메시지.
- **등급별 스프라이트 차등화**: SR 딜러는 모자, SSR 딜러는 왕관 + 반짝이는 스파클 이펙트 추가로 시각적으로 고급스럽게 구분(N/R은 기본 실루엣, 색상으로만 구분).
- 세이브 버전 v2 → v3 (dealerPulls/achievements 필드 추가, 구버전 세이브는 초기화됨).
- `npm run build` 통과 확인.

### 2026-09-11 (5차) — 손님 착석 + 말풍선
- 손님이 실제로 홀덤 테이블에 앉도록 수정: 딜러 유무와 무관하게 모든 활성 테이블에 손님 스프라이트 착석(딜러는 테이블 뒤쪽, 손님은 앞쪽에 배치).
- 랜덤 빈도(3~6.5초 간격)로 임의의 테이블 위에 말풍선을 띄워 홀덤 용어/일반 플레이어 반응 대사(올인/풀하우스/블러핑/레이즈/체크 등 20종)를 랜덤 출력. 말풍선은 위로 떠오르며 페이드아웃.
- 하단 배회 손님(ambient wandering) 로직은 제거 — 이제 손님은 테이블에 고정 착석.
- `npm run build` 통과 확인.

### 2026-09-11 (4차) — 버그 수정 + 홀덤 테마
- **버그**: 딜러 배정 select가 열리자마자 닫히던 문제 → 원인은 `setInterval(hud.refresh)`가 250ms마다 `render()`(innerHTML 전체 재생성)를 호출해서 열려있던 select가 매번 새로 그려져 닫힌 것. `refresh()`를 구조를 건드리지 않는 가벼운 갱신(잔고/수익 텍스트, 버튼 disabled, 진행바 폭만 갱신)으로 분리, `render()`는 상태 변화 이벤트/탭 전환 때만 호출하도록 수정.
- **버그**: 하단 탭 버튼 누를 때마다 게임 캔버스 크기가 흔들리던 문제 → `#hud`가 `max-height`라 탭마다 콘텐츠 높이가 달라지면서 `#game-container`가 flex로 늘었다 줄었다 했던 것. `#hud` 높이를 고정(44vh)하고 내부 `.tab-content`만 스크롤되도록 수정.
- **테마**: "판타지 목재 타이쿤" 느낌에서 실제 홀덤펍/카지노 느낌으로 재조정. 바닥을 와인레드 카펫 톤으로, 테이블은 초록 펠트+금색 테두리로 통일, 벽 장식은 화분 대신 포커 칩 스택으로 교체, 전체 팔레트를 딥와인+골드 톤으로.
- `npm run build` 통과 확인.

### 2026-09-11 (3차)
- 전면 비주얼 리뉴얼: 카이로소프트/메이플스토리 모티브의 따뜻한 색감으로 변경.
- `src/game/pixelart.ts` 신설: 도트 스프라이트를 문자열 격자 + 팔레트로 정의하고 캔버스 텍스처로 굽는 유틸(`ensurePixelTexture`). 손으로 좌표를 세지 않도록 격자 생성 자체도 배열 헬퍼 함수로 처리.
- 실제 캐릭터 스프라이트 도입: 딜러(등급별 조끼 색상 4종), 손님(셔츠 색상 3종) chibi 실루엣, 포커 테이블(나무 테두리+펠트+칩) 스프라이트로 기존 사각형/뱃지 placeholder 교체.
- 바닥은 반복 타일(TileSprite)로 따뜻한 나무 바닥 패턴, 상단에 화분 장식, 화면 하단에 배회하는 손님 2명(장식용, 좌우 왕복 트윈)으로 생동감 추가.
- HUD를 하단 탭 네비게이션 구조로 전면 개편: 상단 잔고바 + 탭 콘텐츠(테이블/딜러/매장) + 최하단 3버튼 네비게이션. 기존 좌우 분할 레이아웃 제거, 세로 스택 고정.
- CSS 팔레트를 나무색+크림+골드+민트 톤으로 전면 교체.
- `npm run build` 통과 확인 (Playwright 브라우저는 이 컨테이너 네트워크 제한으로 설치 불가해 실브라우저 스크린샷 검증은 생략 — 사용자가 배포 링크에서 직접 확인 필요).
- **다음 할 일**: OpenGameArt 등 참고해 스프라이트 디테일(얼굴 표정, 걷기 애니메이션 프레임) 보강, 테이블 슬롯 레이아웃이 최종 등급(16테이블)에서 세로로 넘치는 문제 레이아웃 개선, Capacitor 모바일 패키징, 밸런스 튜닝.

### 2026-09-11 (2차)
- **전직 시스템** 추가 (`src/game/jobs.ts`): 매장 등급 1 도달 시 1차 전직(베테랑 딜러 출신 vs 수완 좋은 사업가) 선택, 등급 2 도달 시 1차 선택에 따라 2차 전직 갈래(하우스 마스터/헤드헌터, 토너먼트 흥행사/VIP 매니저)로 분기. 전직 선택 전에는 매장 확장(프레스티지) 불가하도록 막아둠. 전직 효과는 `jobPath`에 영구 저장되며 프레스티지해도 유지됨.
- **딜러 가챠 시스템** 추가 (`src/game/gacha.ts`): 기존 "딜러 고용" 버튼을 확률 뽑기(N/R/SR/SSR, 가중치 60/28/10/2)로 교체. 등급별로 "보유 효과"(배정 안 해도 적용되는 전체 수익 소량 배율, 누적)와 "배치 효과"(테이블 배정 시 곱연산 배율), 레벨업 보너스 계수가 다르게 스케일링됨. 헤드헌터 전직 시 SR/SSR 가중치 1.5배.
- 도트 스타일 보강: 바닥을 16px 체커 타일로, 테이블 상단에 픽셀 점무늬 트림, 딜러는 등급 색상의 3x3 픽셀 배지로 표시.
- 세이브 버전 v1 → v2로 올림(jobPath 필드 추가, 구버전 세이브는 초기화됨 — 아직 배포 전이라 마이그레이션 생략).
- `npm run build` 통과 확인.
- **다음 할 일**: OpenGameArt 도트 에셋 조사 및 placeholder 교체(CREDITS.md 신설), Capacitor 모바일 패키징, 밸런스 수치 튜닝(특히 가챠 비용/전직 배율), 3차 전직 추가 검토.

### 2026-09-11
- Vite + TypeScript + Phaser 3 스캐폴드 생성, Phaser 게임 설정(pixelArt, FIT 스케일).
- 핵심 방치형 루프 구현: `src/game/`에 매장 등급(balance.ts), 상태/액션(GameState.ts), localStorage 저장+오프라인 수익(SaveManager.ts).
- 매장 등급 4단계 정의: 구석 테이블 하나 → 동네 홀덤 매장 → 지역 카지노 → 국내 최고 카지노. 등급마다 테이블 수/매출/비용 곡선, 확장(프레스티지) 비용과 영구 배율 설정.
- `MainScene`에서 테이블 grid 렌더링(placeholder 사각형, 팀 색상), 탭하면 보너스 수금 + 플로팅 텍스트. 빈 다음 칸을 눌러도 테이블 구매 가능.
- `HUD`(DOM 오버레이)에서 테이블 강화/구매, 딜러 고용/교육/배정, 매장 확장 버튼 제공.
- Playwright로 실제 브라우저에서 구매→고용→배정→강화→프레스티지 전체 플로우 스크린샷 검증 완료 (핵심 루프 정상 동작 확인).
- **다음 할 일**: OpenGameArt 도트 에셋 조사 및 placeholder 교체(CREDITS.md 신설), Capacitor 모바일 패키징, 밸런스 수치 튜닝.

## 참고

- 기술 스택 선택: Phaser 3 + Capacitor (사용자가 직접 선택, 이유: 브라우저에서 바로 실행/스크린샷 검증 가능해 반복 개발이 빠름).
- 저장 포맷 버전은 `src/game/SaveManager.ts`의 `SAVE_VERSION`. 세이브 스키마(`GameSaveData`)를 바꿀 때는 버전을 올리고 마이그레이션 여부를 검토할 것.
