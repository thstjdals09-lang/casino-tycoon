# Casino Tycoon 작업 메모

새 세션을 시작하면 아래 "진행 상황 로그"부터 읽고 이어서 작업할 것. 세션을 마칠 때는 맨 위에 날짜와 함께 새 항목을 추가한다 (무엇을 했는지 / 다음에 할 일).

## 진행 상황 로그

### 2026-09-11 (13차) — 초기화 버튼 추가
- 참고: "딜러 다 날아갔다"는 문제는 12차에서 고친 예전 프레스티지 리셋 동작 때문이었을 가능성이 높음(그 전엔 매장 확장 시 딜러가 전부 사라졌음). 지금은 고쳐져서 확장해도 딜러가 유지됨.
- 세이브가 브라우저 localStorage라 서버/코드 쪽에서 직접 지워줄 수 없어서, 매장 탭 맨 아래에 "처음부터 다시 시작(전체 초기화)" 버튼을 추가함. 확인창 뜨고 동의하면 세이브 삭제 후 새로고침.
- `npm run build` 통과 확인.

### 2026-09-11 (12차) — 프레스티지(초기화) 대신 누적 성장 구조로 변경
- 사용자 요청: "전생/리셋 시스템 대신 누적해서 수익을 버는 구조가 좋아 보인다" → 확인 결과 "초기화 없이 누적 성장(테이블/딜러/디자인/바 유지, 등급만 오름)" 방식 선택.
- **매장 확장 조건 변경**: 이전엔 현금이 advanceCost 이상이면 아무 때나 확장 가능했음 → 이제 "현재 층 테이블을 꽉 채워야"(tables.length >= maxTables) + 현금 충분해야 확장 가능. 즉 "테이블 수가 늘어나다가 맵이 꽉 차면 다음 층으로" 흐름.
- **확장해도 리셋 없음**: 현금(비용만 차감), 테이블, 딜러, 인테리어 레벨, 바 레벨 전부 그대로 유지한 채 venueTierIndex만 증가. prestigeMultiplier 보너스는 그대로 적용. 새 층은 maxTables가 더 커서 테이블을 더 살 수 있는 자리가 열리고, tableBaseIncome 등 티어 수치가 올라가 기존 테이블 수익도 함께 오름.
- HUD 매장 탭: 테이블이 안 찼으면 "테이블을 꽉 채우면 확장 가능" 안내, 다 찼으면 기존처럼 현금 진행바 + 확장 버튼(문구에서 "레벨 초기화" 문구 제거).
- `npm run build` 통과 확인. (세이브 포맷 변경 없음)

### 2026-09-11 (11차) — 강화 버튼 누르면 스크롤 위치 튀는 문제 수정
- 강화/교육 등 버튼을 눌러 상태가 바뀌면 `render()`가 `.tab-content`의 innerHTML을 통째로 교체하면서 스크롤이 맨 위로 리셋되던 문제 수정. 이제 렌더 직전 스크롤 위치를 저장했다가 렌더 후 그대로 복원. 단, 하단 탭(테이블/딜러/매장)을 새로 눌러 전환한 경우엔 새 탭이니 스크롤을 맨 위로 리셋.
- `npm run build` 통과 확인.

### 2026-09-11 (10차) — 진짜 원인: 딜러/테이블 ID 뒤바뀜 버그
- **진짜 버그**: `onChange` 핸들러에서 select의 `data-id`(테이블 id)를 딜러 id로, `value`(딜러 id)를 테이블 id로 거꾸로 읽고 있었음. 그래서 드롭다운에서 아무 딜러나 선택해도 내부적으로 존재하지 않는 테이블 id를 참조하게 돼 조용히 아무 일도 안 일어났음 (9차에서 고친 "목록에 안 보이는" 문제와는 별개의 훨씬 근본적인 버그였음 — 스크린샷으로 재현 확인 후 발견).
- `tableId = data-id`, `dealerId = value`로 순서 수정. "딜러 없음" 선택 케이스도 명시적으로 처리.
- `npm run build` 통과 확인.

### 2026-09-11 (9차) — 딜러 배정 드롭다운 버그 수정
- **버그**: 이미 다른 테이블에 배정된 딜러는 드롭다운 목록에서 아예 안 보이게 필터링돼 있었음. 그래서 보유한 딜러가 전부 다른 테이블에 배정된 상태에서 새 테이블에 배정하려 하면 "딜러 없음"만 보여서 아무것도 고를 수 없었음(= "딜러 교체가 안 된다"/"손님이 안 앉는다"의 원인).
- 수정: 모든 테이블의 드롭다운에 보유한 딜러 전체를 표시하고, 각 옵션에 현재 배정 상태(대기 중 / 이 테이블 / 테이블 #N에서 이동)를 라벨로 표시. 다른 테이블에 배정된 딜러를 선택하면 그 테이블에서 자동으로 빠지고 여기로 이동(기존 `assignDealer` 로직이 이미 스왑을 지원했음, UI에서 막고 있었던 것).
- `npm run build` 통과 확인.

### 2026-09-11 (8차) — 초반 밸런스 대폭 개선 (너무 느리다는 피드백 반영)
- 1티어(구석 테이블) 수치 전면 하향/상향 조정: 테이블 기본수익 1→3, 첫 테이블 구매비 50→25, 딜러 가챠 비용 80→35, 매장 확장 비용 5000→1200, 무딜러 효율 0.25→0.35. 체감상 시작하고 1분 이내에 딜러 뽑고, 몇 분 안에 첫 매장 확장(프레스티지)까지 가능하도록 튜닝.
- 2티어/3티어도 비례해서 완화(확장비 80000→25000, 1.5M→900K 등).
- 탭 쿨다운 3초→2초로 단축해 능동 플레이 피드백 속도 개선.
- `npm run build` 통과 확인. (세이브 포맷 변경 없음, 기존 세이브 유지됨)

### 2026-09-11 (7차) — 손님 등급/인테리어/미니바/도감 필터
- **손님 등급 시스템** 신설 (`src/game/customers.ts`): C(일반)/B(단골)/A(큰손)/S(VIP) 4등급, 등급별로 테이블 수익 배율(spendMultiplier)과 바 매출 배율(drinkMultiplier)이 다름. 딜러가 테이블에 배정될 때 현재 매장 디자인 레벨을 반영해 손님 등급을 추첨.
- **매장 인테리어(디자인) 업그레이드** (`src/game/decor.ts`): 매장 탭에서 구매 가능, 레벨이 오를수록 고급 손님(B/A/S) 등장 확률 상승. 매장 확장 시 초기화.
- **미니바 시스템**: 매장 탭에서 업그레이드, 레벨이 오를수록 더 비싼 음료 판매(초당 매출 자동 발생, 착석한 손님 등급에 따라 매출 배율 상승). 매장 확장 시 초기화.
- **딜러 도감 개편**: 딜러 탭에 등급별 보유효과/배치효과/레벨업 배율을 모두 텍스트로 표시. 보유(누적 고용 1명 이상)한 등급은 컬러, 미보유 등급은 무채색+자물쇠 아이콘. 보유/미보유, 등급(N/R/SR/SSR) 필터 칩 추가.
- 테이블 탭에 착석한 손님 등급 배지 표시.
- 도트: 손님도 등급별 텍스처(색상 구분, S등급은 모자+반짝이)로 분리, 미니바 카운터(병+선반) 스프라이트 추가해 화면 상단에 고정 배치.
- 세이브 버전 v3 → v4 (customerGrade/designLevel/barLevel 필드 추가, 구버전 세이브는 초기화됨).
- `npm run build` 통과 확인.

### 2026-09-11 (6차) — 도감/업적 + 손님 착석 조건 수정 + 등급별 스프라이트 차등화
- 손님 착석 조건 수정: 딜러가 배정된 테이블에만 손님이 앉도록 원복(이전엔 딜러 유무 무관하게 전부 착석했었음). 말풍선도 딜러 있는 테이블에서만 뜨도록 함께 수정.
- **손님 착석 수익 보너스**: 딜러 배정(=손님 착석) 시 테이블 수익에 +15% 배율 추가 (`CUSTOMER_SEATED_BONUS`).
- **딜러 도감/업적 시스템** 신설 (`src/game/achievements.ts`): 등급별/누적 고용 수 기준 업적 7종(N 5명, R 5명, SR 3명, SSR 1명, SSR 3명, 누적 20명, 누적 50명), 달성 시 영구 전체 수익 배율 적용. 누적 고용 카운트(`dealerPulls`)는 프레스티지해도 초기화 안 됨. HUD 딜러 탭 하단에 도감 목록/진행 현황 표시, 새로 달성하면 딜러 탭에 플래시 메시지.
- **등급별 스프라이트 차등화**: SR 딜러는 모자, SSR 딜러는 왕관 + 반짝이는 스파클 이펙트 추가로 시각적으로 고급스럽게 구분(N/R은 기본 실루엣, 색상으로만 구분).
- 세이브 버전 v2 → v3 (dealerPulls/achievements 필드 추가, 구버전 세이브는 초기화됨).
- `npm run build` 통과 확인.

### 2026-09-11 (5차) — 손님 착석 + 말풍선
- 손님이 실제로 홀덤 테이블에 앉도록 수정: 딜러 유무와 무관하게 모든 활성 테이블에 손님 스프라이트 착석(딜러는 테이블 뒤쪽, 손님은 앞쪽에 배치).
- 랜덤 빈도(3~6.5초 간격)로 임의의 테이블 위에 말풍선을 띄워 홀덤 용어/일반 플레이어 반응 대사(올인/풀하우스/블러핑/레이즈/체크 등 20종)를 랜덤 출력. 말풍선은 위로 떠오르며 페이드아웃.
- 하단 배회 손님(ambient wandering) 로직은 제거 — 이제 손님은 테이블에 고정 착석.
- `npm run build` 통과 확인.

### 2026-09-11 (4차) — 버그 수정 + 홀덤 테마
- **버그**: 딜러 배정 select가 열리자마자 닫히던 문제 → 원인은 `setInterval(hud.refresh)`가 250ms마다 `render()`(innerHTML 전체 재생성)를 호출해서 열려있던 select가 매번 새로 그려져 닫힌 것. `refresh()`를 구조를 건드리지 않는 가벼운 갱신(잔고/수익 텍스트, 버튼 disabled, 진행바 폭만 갱신)으로 분리, `render()`는 상태 변화 이벤트/탭 전환 때만 호출하도록 수정.
- **버그**: 하단 탭 버튼 누를 때마다 게임 캔버스 크기가 흔들리던 문제 → `#hud`가 `max-height`라 탭마다 콘텐츠 높이가 달라지면서 `#game-container`가 flex로 늘었다 줄었다 했던 것. `#hud` 높이를 고정(44vh)하고 내부 `.tab-content`만 스크롤되도록 수정.
- **테마**: "판타지 목재 타이쿤" 느낌에서 실제 홀덤펍/카지노 느낌으로 재조정. 바닥을 와인레드 카펫 톤으로, 테이블은 초록 펠트+금색 테두리로 통일, 벽 장식은 화분 대신 포커 칩 스택으로 교체, 전체 팔레트를 딥와인+골드 톤으로.
- `npm run build` 통과 확인.

### 2026-09-11 (3차)
- 전면 비주얼 리뉴얼: 카이로소프트/메이플스토리 모티브의 따뜻한 색감으로 변경.
- `src/game/pixelart.ts` 신설: 도트 스프라이트를 문자열 격자 + 팔레트로 정의하고 캔버스 텍스처로 굽는 유틸(`ensurePixelTexture`). 손으로 좌표를 세지 않도록 격자 생성 자체도 배열 헬퍼 함수로 처리.
- 실제 캐릭터 스프라이트 도입: 딜러(등급별 조끼 색상 4종), 손님(셔츠 색상 3종) chibi 실루엣, 포커 테이블(나무 테두리+펠트+칩) 스프라이트로 기존 사각형/뱃지 placeholder 교체.
- 바닥은 반복 타일(TileSprite)로 따뜻한 나무 바닥 패턴, 상단에 화분 장식, 화면 하단에 배회하는 손님 2명(장식용, 좌우 왕복 트윈)으로 생동감 추가.
- HUD를 하단 탭 네비게이션 구조로 전면 개편: 상단 잔고바 + 탭 콘텐츠(테이블/딜러/매장) + 최하단 3버튼 네비게이션. 기존 좌우 분할 레이아웃 제거, 세로 스택 고정.
- CSS 팔레트를 나무색+크림+골드+민트 톤으로 전면 교체.
- `npm run build` 통과 확인 (Playwright 브라우저는 이 컨테이너 네트워크 제한으로 설치 불가해 실브라우저 스크린샷 검증은 생략 — 사용자가 배포 링크에서 직접 확인 필요).
- **다음 할 일**: OpenGameArt 등 참고해 스프라이트 디테일(얼굴 표정, 걷기 애니메이션 프레임) 보강, 테이블 슬롯 레이아웃이 최종 등급(16테이블)에서 세로로 넘치는 문제 레이아웃 개선, Capacitor 모바일 패키징, 밸런스 튜닝.

### 2026-09-11 (2차)
- **전직 시스템** 추가 (`src/game/jobs.ts`): 매장 등급 1 도달 시 1차 전직(베테랑 딜러 출신 vs 수완 좋은 사업가) 선택, 등급 2 도달 시 1차 선택에 따라 2차 전직 갈래(하우스 마스터/헤드헌터, 토너먼트 흥행사/VIP 매니저)로 분기. 전직 선택 전에는 매장 확장(프레스티지) 불가하도록 막아둠. 전직 효과는 `jobPath`에 영구 저장되며 프레스티지해도 유지됨.
- **딜러 가챠 시스템** 추가 (`src/game/gacha.ts`): 기존 "딜러 고용" 버튼을 확률 뽑기(N/R/SR/SSR, 가중치 60/28/10/2)로 교체. 등급별로 "보유 효과"(배정 안 해도 적용되는 전체 수익 소량 배율, 누적)와 "배치 효과"(테이블 배정 시 곱연산 배율), 레벨업 보너스 계수가 다르게 스케일링됨. 헤드헌터 전직 시 SR/SSR 가중치 1.5배.
- 도트 스타일 보강: 바닥을 16px 체커 타일로, 테이블 상단에 픽셀 점무늬 트림, 딜러는 등급 색상의 3x3 픽셀 배지로 표시.
- 세이브 버전 v1 → v2로 올림(jobPath 필드 추가, 구버전 세이브는 초기화됨 — 아직 배포 전이라 마이그레이션 생략).
- `npm run build` 통과 확인.
- **다음 할 일**: OpenGameArt 도트 에셋 조사 및 placeholder 교체(CREDITS.md 신설), Capacitor 모바일 패키징, 밸런스 수치 튜닝(특히 가챠 비용/전직 배율), 3차 전직 추가 검토.

### 2026-09-11
- Vite + TypeScript + Phaser 3 스캐폴드 생성, Phaser 게임 설정(pixelArt, FIT 스케일).
- 핵심 방치형 루프 구현: `src/game/`에 매장 등급(balance.ts), 상태/액션(GameState.ts), localStorage 저장+오프라인 수익(SaveManager.ts).
- 매장 등급 4단계 정의: 구석 테이블 하나 → 동네 홀덤 매장 → 지역 카지노 → 국내 최고 카지노. 등급마다 테이블 수/매출/비용 곡선, 확장(프레스티지) 비용과 영구 배율 설정.
- `MainScene`에서 테이블 grid 렌더링(placeholder 사각형, 팀 색상), 탭하면 보너스 수금 + 플로팅 텍스트. 빈 다음 칸을 눌러도 테이블 구매 가능.
- `HUD`(DOM 오버레이)에서 테이블 강화/구매, 딜러 고용/교육/배정, 매장 확장 버튼 제공.
- Playwright로 실제 브라우저에서 구매→고용→배정→강화→프레스티지 전체 플로우 스크린샷 검증 완료 (핵심 루프 정상 동작 확인).
- **다음 할 일**: OpenGameArt 도트 에셋 조사 및 placeholder 교체(CREDITS.md 신설), Capacitor 모바일 패키징, 밸런스 수치 튜닝.

## 참고

- 기술 스택 선택: Phaser 3 + Capacitor (사용자가 직접 선택, 이유: 브라우저에서 바로 실행/스크린샷 검증 가능해 반복 개발이 빠름).
- 저장 포맷 버전은 `src/game/SaveManager.ts`의 `SAVE_VERSION`. 세이브 스키마(`GameSaveData`)를 바꿀 때는 버전을 올리고 마이그레이션 여부를 검토할 것.
